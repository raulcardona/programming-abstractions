*/  File: 03_04.cpp
*   --------------------------
*   This program will implement a function
*   capitalize(str) where the input string will 
*   be returned with the first letter capitalized.
*/

#include <iostream>
#include <string>
#include "simpio.h"

/* Function Prototypes */
string capitalize(string str);

int main() {

return 0;
}

string capitalize(string str) {
  int length = str.length();
  for (int i = 0; i < length; i++) {
    if (str.at(i) == 0) {
        str.at(i) = str.at(i - )
      }
  
  }
}
